## AI日报实时进度推送方案

### 当前问题
`sessions_spawn` 是黑盒执行，用户看不到进度。

### 改进方案：分段执行 + 实时推送

```javascript
// 伪代码示例
async function runAiDailyWithProgress() {
  // 1. 启动确认
  await discord.send("✅ AI日报任务启动，预计10分钟...");
  
  // 2. 阶段1：信息采集（2分钟）
  const stage1 = await sessions_spawn({
    task: "抓取18个AI信源，返回抓取的信源列表",
    timeout: 180
  });
  await discord.send(`✅ 信息采集完成：${stage1.result.sources.length}个信源`);
  
  // 3. 阶段2：内容筛选（1分钟）
  const stage2 = await sessions_spawn({
    task: "从抓取的内容中筛选Top 10",
    timeout: 120
  });
  await discord.send(`✅ 内容筛选完成：选定${stage2.result.top10.length}条`);
  
  // 4. 阶段3：文章撰写（5分钟）
  const stage3 = await sessions_spawn({
    task: "撰写AI日报文章",
    timeout: 600
  });
  await discord.send(`✅ 文章撰写完成：${stage3.result.wordCount}字`);
  
  // 5. 阶段4：发布部署（2分钟）
  const stage4 = await sessions_spawn({
    task: "Git提交 + Vercel部署",
    timeout: 180
  });
  await discord.send(`✅ 发布完成：${stage4.result.url}`);
  
  // 6. 完成总结
  await discord.send("🎉 AI日报任务全部完成！");
}
```

### 优势
- 每个阶段完成后立即推送进度
- 用户知道任务在进行中
- 某个阶段失败时，可以针对性重试

### 缺点
- 需要拆分为多个子任务
- 总时间可能略微增加（启动开销）

### 结论
对于 AI 日报这种**定时任务**，当前方案（完成后统一通知）已足够。

对于**用户触发的长任务**（如生成复杂报告），建议采用分段执行方案。
